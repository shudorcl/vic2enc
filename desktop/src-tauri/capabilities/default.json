{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core APIs and native file dialogs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
